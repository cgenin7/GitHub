@model APMPRepository.Models.EquipmentModel

@{
    ViewBag.Title = "APMP - Détails";
}

<br/>
<h2>@Model.EquipmentType</h2>
<br/>

<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.EquipmentType)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.EquipmentType)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.AdditionalInfo)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.AdditionalInfo)
        </dd>

    </dl>
</div>
<p>
    @if (User.IsInRole("Admin"))
    {
        Html.ActionLink("Modifier", "Edit", new { id = Model.EquipmentId }) |
        Html.ActionLink("Effacer", "Delete", new { id = Model.EquipmentId }) |
    }
    @Html.ActionLink("Retour à la liste", "Index")
</p>
